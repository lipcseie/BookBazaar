@* @model List<Product>

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="lead text-primary">
                CATEGORY LIST
            </h2>
        </div>
        <div class="small col-6 text-end">
            <a asp-controller="Product" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> CREATE NEW CATEGORY
            </a>
        </div>
    </div>

<table class="table table-bordered table-striped">
    <thead>
    <tr>
        <th>
            CATEGORY NAME
        </th>
        <th>
            DISPALY ORDER
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        @foreach(var product in Model.OrderBy(u => u.DisplayOrder))
        {
            
        <tr>
            <td>
             @product.Name
            </td>
            <td>
             @product.DisplayOrder
            </td>
            <td>
                 <div class=" w-75 btn-group" role="group">
                            <a asp-controller="Product" asp-action="Edit" asp-route-id="@product.Id" class="btn btn-primary mx-2 rounded-end">
                                <i class="bi bi-pencil pe-1"></i>Edit
                     </a>

                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id" class="btn btn-secondary mx-2 rounded-start">
                                <i class="bi bi-trash-fill pe-1"></i>Delete
                            </a>
                 </div>

            </td>
        </tr>
        }
    </tbody>
</table>
</div> *@